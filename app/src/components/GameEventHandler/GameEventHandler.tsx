"use server";

import LogReader from '@/utils/classes/LogReader';
import { useContext } from 'react';

type Props = {
  // children: React.ReactNode,
}

export default async function GameEventHandler({}:Props) {
  
  return null;
}